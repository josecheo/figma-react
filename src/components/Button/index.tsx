import React from 'react';
import { ButtonProps } from './Button.types';

const Button = () => {

  return (
    <button
      type="button"
    >
     asdasd
    </button>
  );
};
 export default Button;